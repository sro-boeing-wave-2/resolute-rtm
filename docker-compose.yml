version: "3.1"
services:
    web:
      build: RTM-Chat/
      ports:
        - "8084:5000"
      depends_on:
        - mongo
    mongo:
      image: mongo:4.1
      restart: always
      environment:
        MONGO_INITDB_DATABASE: MongoConversation
      ports:
        - "27020:27017"